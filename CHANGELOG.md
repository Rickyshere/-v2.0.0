# 更新日志

本文件记录各版本的主要变更。数据规模类数字均由代码实测得出，可复现。

---

## v2.0.0

> 一次大版本更新：新增**高速动车组（G/D/C）独立页面**、**票价查询**、**推荐中转方案**，
> 并把普速路网的多处数据缺陷一次性修掉。

### ✨ 新功能

#### 1. 高速动车组页面（八纵八横，车站级）
页面顶部新增「**普速列车 / 高速动车组**」切换，高速页面：

- 路网依据 2016《中长期铁路网规划》的**八纵八横** 16 条主通道，按「是否通车」
  拆成 **35 段**（30 段参与算路 + 5 段 `[在建]`），共 **641 个车站 / 691 条连接**
- 车次只查 **G / D / C** 字头，与普速页面的 K / Z / T / Y / 纯数字完全隔离
- **功能与普速页面完全一致**：两个页面共用同一套渲染与交互逻辑，只切换
  「数据源 + 车次类别」，因此方案查询、地图、车次、直达、票价、已选车次、
  推荐方案、通道总览全部可用

#### 2. 票价查询（可选，勾选后启用）
表单新增「**增加票价查询功能**」勾选框，车次列表每行附带
**无座 / 硬座 / 硬卧 / 软卧 / 二等座 / 一等座 / 商务座** 票价。

- 票价直接解码 12306 返回中的 `yp_info` 字段，**不产生额外请求**
- 无座按铁路规则（= 硬座价 / 二等座价）推导，以 `*` 标注为推导值
- 「直达车次」与「推荐方案」里的票价**默认折叠**，点开才显示

#### 3. 推荐方案（直达 / 中转）
新增「**推荐方案**」按钮：先查直达，再用路网图找候选枢纽拼中转，
只保留**接续时间 ≥ 60 分钟**的组合（可调），按总耗时排序。

- 支持**次日接续** —— 普速车常跨天，只允许当日接续会让普速中转几乎全部判为不可行
- 候选枢纽取路网图上的**换乘大站**，而非路径上所有中间站

#### 4. 在线地理坐标补全（网页内一键完成）
「线路管理」新增开关「**是否启用在线地理坐标补全，需提供百度地图开放平台服务端AK**」。

- Key 通过**系统环境变量** `BAIDU_AK` 传入，项目内不硬编码任何 Key
- **仅在确实存在缺坐标站点时才发起请求**，坐标齐全时不消耗额度
- 并发**硬上限 3**（百度对同一 AK 的限制），可用环境变量调低但无法调高

#### 5. 同城异站自动归并
- **自动**：站名有前缀关系（芜湖 / 芜湖南）且距离 ≤ 25km → 自动归并为同一城市
- **手工**：`same_city.json` 补充"站名无前缀关系但确属同城"的站对（中华门 / 南京）
- 「线路管理」里可查看自动归并结果与**待确认的邻近站对**

#### 6. 动态加站表单（补回）
「线路管理」底部新增「动态加站（临时）」：填站名 + 经纬度 + 等级即可加入路网，
自动接入最近的大城。⚠ 只在内存中生效，重启服务后失效。

### 🔧 重要修复

| 问题 | 影响 |
|---|---|
| **同城站点漏归并导致走廊断裂** | 南京→九江 被迫绕行 信阳·武昌 算成 **813km**（实际约 380km）；补齐 11 对同城站点后修正为 **379km** |
| **坐标无效的站点不接入路网** | 缺坐标站点被写成 `0,0`（几内亚湾），曾让「中华门→北京」算出 **12593km** 的荒唐结果 |
| **坐标优先级顺序错误** | 生成的 `stations.csv` 会**永久遮蔽** `extra_coords.json` 里的修正，导致改了缓存也不生效。改为 `CITY_FALLBACK > extra_coords.json > stations.csv` |
| **线路总览不刷新** | 新增线路在总览里看不到（`lineOverview` 只在首次打开时拉取，从不作废） |
| **线路管理编辑框覆盖外部改动** | 用记事本改了 `main_railway_line.txt` 后回网页点保存，会拿旧文本**整份覆盖** |
| **解析器产生"幽灵站"** | `金温铁路（金华—温州）：…` 这类写法会把线名当成站点。三处重复的解析器统一复用同一个 |
| **高速页面推荐方案 404** | 前端把 URL 拼成 `/api/hsr/recommend`，后端只有 `/api/recommend` |
| **票价/车次显示"当日不开行"的车次** | 12306 返回的 `24:00 / 99:59` 占位值被当成真实车次列出 |
| **「汉中」被地理编码抓错地点** | 被解析到「佛坪」县城（相距仅 **1.26km**），导致 西安北→成都东 算成 774km（真实 658km，+17.6%）。修正后 **633km（-3.8%）** |

### 📊 数据

| 项目 | v1.4 | v2.0.0 |
|---|---|---|
| 普速站点 | 223 | **241** |
| 高铁车站 | — | **641**（35 段通道） |
| 电报码对照 | 219 条 | **275 条** |
| 坐标缓存 | 67 条 | **685 条** |
| API 接口 | 15 | **23** |
| 前端测试 | 无 | **46 项断言 + 17 条路由** |

- **电报码**：补全 21 个缺失站的码（此前这些站查不到 12306 车次），
  并在「线路管理」里显示覆盖率与无码站点
- **坐标复核（两道检查）**：
  - `find_suspect_edges()` —— 扫"相邻站间距异常大"的区段
  - `find_suspect_duplicates()` —— 扫"几乎重合但名字无关"的站
  两者都是为发现被地理编码**解析到异地同名**的错误坐标。
  实测抓到过：「马鞍山」→ 广东、「雄安」→ 广东、「汉中」→ 佛坪县城。
  > 为什么需要第二道：汉中→宁强南 的 173.6km **落在合法长区间范围内**
  > （兰新高铁柳园南—哈密 就有 253km），单靠距离阈值抓不到；
  > 而"与佛坪仅相距 1.26km"这个信号则非常干净（实测零误报）。

### 🧪 工程

- **建立 git 仓库与基线**，早期试验脚本移入 `legacy/`，新增 `.gitignore` / `.gitattributes`
- **前端回归测试**（jsdom，可选开发依赖，不进 Python 依赖）：
  - `tests/frontend/features_test.js` —— **46 项断言**，覆盖页面切换、车次类别、
    票价开关与折叠、推荐方案、收藏徽标、动态加站
  - `tests/frontend/routes_test.js` —— **17 条路径**，把前端请求过的 URL 与后端
    路由表做集合比对，报出任何会 404 的路径

### ⚠️ 升级注意（行为变更）

1. **坐标优先级变了**：`extra_coords.json` 现在**高于**生成的 `stations.csv`。
   此前在 `extra_coords.json` 里做的修正一直不生效，现在会生效。
2. **缺坐标站点不再接入路网**：此前会被当成 `(0,0)` 参与算路并产生假边。
   现在这类站点不参与算路，并在网页上被列出。
3. **高速页面的在建段默认不参与算路**：可调 `POST /api/hsr/reload?include_planned=true` 打开。
4. **里程是近似值，且偏差方向不定**：普速与高铁的里程都是「相邻站直线距离之和」。
   实测 15 条主要干线：**-19% ~ +15%，平均 -7%**，其中 8 条在 ±10% 以内。
   高铁车站级化之前（城市级骨架）偏差更大。**准确里程与时刻一律以 12306 为准。**
5. **部分高速通道段仍在建**：数据里收录了规划站点（标 `[在建]`），12306 查不到车次。
6. **改动代码后需重启服务**：内存中的路网不会自动重载（除线路清单可热重载）。

### 已知限制

- 八纵八横车站表为**人工整理的主要车站**，经「相邻站间距」自动校验，
  但**不代表官方完整站表**：部分县级小站、近年新开车站可能未收录
- `台北`、`澳门` 属规划中节点，12306 暂不发售
- 推荐方案会查 `1 + 2~3 × 候选枢纽数` 次 12306，受串行限速影响**约需 10 秒**
- 动态加站**只在内存中生效**，重启后失效；长期保留需写进 `main_railway_line.txt`
